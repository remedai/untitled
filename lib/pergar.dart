import 'package:flutter/material.dart';
class fire_fire extends StatefulWidget {
  const fire_fire({Key? key}) : super(key: key);

  @override
  _fire_fireState createState() => _fire_fireState();
}

class _fire_fireState extends State<fire_fire> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:SingleChildScrollView(
        scrollDirection: Axis.vertical,
        child:Center (
          child: Column(
            children: [
              Stack(
                children: [



                ],



              )


            ],

          ),




        ))


    );
  }
}
